apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fronend-hpa
  namespace: blood-bank
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: frontend
    selector:
      matchLabels:
        app: frontend
  minReplicas: 1
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 3
